<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product</title>
    <!-- Add Tailwind CSS CDN link -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">
    <div class="bg-blue-500 p-4 text-white">
        <h1 class="text-2xl font-semibold">Add Product</h1>
    </div>

    

    <div class="p-4">
        <a href="/products" class="text-blue-500 hover:underline">Back to Products</a>
    </div>

    <div style="text-align: center;" class=" mb-8">
        <% if (error.length > 0) { %>
            <% error.forEach(error => { %>
                <div class="bg-red-100 border-red-500 text-red-700 px-4 py-1 rounded relative mb-4" role="alert"> 
                    <strong class="font-bold">Error!</strong>
                    <span class="block sm:inline"><%= error %></span>
                </div>
            <% }) %>
        <% } %>
    </div>

    <div class="container mx-auto">
        <form action="/products/add" method="POST" enctype="multipart/form-data" class="bg-white shadow-md rounded px-4 py-6 sm:px-8 sm:py-8 md:w-1/2 lg:w-1/3 mx-auto">
            <div class="mb-4">
                <label for="name" class="block text-gray-700 text-sm font-bold mb-2">Product Name</label>
                <input type="text" name="name" id="name" placeholder="Enter product name" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
            </div>
            <div class="mb-4">
                <label for="price" class="block text-gray-700 text-sm font-bold mb-2">Product Price</label>
                <input type="number" name="price" id="price" placeholder="Enter product price" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
            </div>
            <div class="mb-4">
                <label for="description" class="block text-gray-700 text-sm font-bold mb-2">Product Description</label>
                <input type="text" name="description" id="description" placeholder="Enter product description" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
            </div>
            <div class="mb-4">
                <label for="image" class="block text-gray-700 text-sm font-bold mb-2">Product Image</label>
                <input type="file" name="productImg" class="block py-2 px-3 border border-gray-300 rounded focus:outline-none focus:shadow-outline">
            </div>
            <div class="mb-4">
                <div class="mb-2">
                    <label for="stock" class="block text-gray-700 text-sm font-bold mb-2">Product Stock</label>
                    <input type="number" name="stock" id="stock" placeholder="Enter product stock" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
                </div>
                <label for="category" class="block text-gray-700 text-sm font-bold mb-2">Product Category</label>
                <% categories.forEach(category => { %>
                    <div class="flex items-center mb-2">
                        <input type="radio" name="category" id="category" value="<%= category.id %>" class="mr-2">
                        <label for="category" class="text-gray-700"><%= category.name %></label>
                    </div>
                <% }) %>
            </div>
            <div class="mb-4">
                <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full focus:outline-none focus:shadow-outline">Add Product</button>
            </div>
        </form>
    </div>
</body>
</html>