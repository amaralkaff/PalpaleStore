<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Detail</title>
    <!-- Add Tailwind CSS CDN link -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">
    <div class="bg-blue-500 p-4 text-white">
        <h1 class="text-2xl font-semibold">Product Detail</h1>
    </div>

    <div class="p-4">
        <a href="/products" class="ml-4 text-blue-500 hover:underline">Back to Products</a>
    </div>

    <div class="container mx-auto p-4">
        <h2 class="text-3xl font-semibold mb-4"><%= data.name %></h2>
        <img src="<%= data.productImg %>" alt="<%= data.name %>" class="mb-4 w-full max-w-lg mx-auto">

        <p class="mb-4">Description: <br> <%= data.description %></p>
        <div class="mb-4">
            <p class="mb-4 font-semibold">Category:</p>
            <% data.Categories.forEach(el => { %>
                <p>><%= el.name %></p>
            <% }) %>
        </div>
        <p class="mb-4"><%= formatNumber(data.price) %></p>
        
        <% if (role === 'admin') { %>
            <p class="font-semibold">Stock: <%= data.stock %></p>
            <a href="/Product/<%= data.id %>" style="text-align: center;" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4 block text-sm">Restock</a>
        <% } else { %>
            <div class="mt-4">
                <a href="/checkout" style="text-align: center;" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded block">Buy</a>
            </div>
        <% } %>
    </div>
</body>
</html>
